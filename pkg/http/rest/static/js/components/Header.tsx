import { Box } from "@chakra-ui/react";
import React from "react";

const Header = () => {
  return (
    <Box layerStyle="padding" m={2}>
      WebsiteChangeNotifier (Excluder)
    </Box>
  );
};

export default Header;
